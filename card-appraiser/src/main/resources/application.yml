spring:
  application:
    name: microservice-card-review

server:
  port: 0

eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true
    ip-address : localhost